import styled from "styled-components";

const Page = styled.div`
  .lists{
    margin-top: -120px;
  }

  @media(max-width: 780px){
    .lists{
      margin-top: -180px;
    }
  }
`;

export default Page;
